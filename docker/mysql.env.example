# Values are deliberately local-only and are supplied only to the MySQL service.
MYSQL_DATABASE=innovation_news
MYSQL_USER=innovation_local
MYSQL_PASSWORD=local-only-database-password
MYSQL_ROOT_PASSWORD=local-only-root-password
